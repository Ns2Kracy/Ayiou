---
title: Ayiou Console 与插件管理
description: 使用内嵌 Console 查看插件状态、启用或停用插件，并通过统一入口热重载支持的插件
---

Ayiou Console 是可选的内嵌用户界面，用来查看插件状态、启用或停用插件、检查健康状态与最近错误，并对支持热重载的插件执行 reload。

Console 背后仍然是 token 保护的 runtime control API。当前 Rust/inventory 插件支持启用和停用，但不支持代码热替换，因为它们已经编译进进程；后续 WASM/Rhai 插件会接入同一套 `reload` 入口。

## 启用 feature

```toml
ayiou = { git = "https://github.com/Ns2Kracy/Ayiou.git", features = ["adapter-console", "embedded-webui"] }
```

只需要 HTTP API 时启用：

```toml
ayiou = { git = "https://github.com/Ns2Kracy/Ayiou.git", features = ["control-plane"] }
```

## 启动 Console

`token` 必填。`bind` 可选，默认是 `127.0.0.1:32187`。

```rust
use ayiou::{ConsoleBot, ControlPlaneOptions};

#[tokio::main]
async fn main() {
    ConsoleBot::console()
        .control_plane(ControlPlaneOptions::new().token("change-me"))
        .run()
        .await;
}
```

自定义监听地址：

```rust
ControlPlaneOptions::new()
    .bind("127.0.0.1:39000")
    .token("change-me")
```

如果没有设置 token，Console 与 runtime control API 不会启动，Bot 会记录明确的配置错误并停止启动。

## 访问 Console

启用 `embedded-webui` 后打开：

```text
http://127.0.0.1:32187/
```

页面会要求输入 token。token 存在浏览器 local storage 中，方便本地开发调试。

Console 支持：

- 查看插件名称、说明、版本、用户状态
- 查看健康状态、最近错误、配置状态
- 启用、停用插件
- 对支持热重载的插件执行 reload
- 查看 advanced capabilities、services、raw runtime state

## HTTP API

所有 API 都要求：

```http
Authorization: Bearer <token>
```

接口：

```text
GET  /api/runtime
GET  /api/plugins
GET  /api/plugins/:id
POST /api/plugins/:id/enable
POST /api/plugins/:id/disable
POST /api/plugins/:id/start
POST /api/plugins/:id/stop
POST /api/plugins/:id/reload
```

Console 的主路径只调用 enable、disable 和 reload。`start` / `stop` 是高级 runtime-control 端点，面向调试或内部编排，不是正常用户概念。

成功响应使用 `ok` / `data` envelope；`data` 的结构按 endpoint 变化。动作接口（enable / disable / start / stop / reload）返回被操作的插件实例：

```json
{
  "ok": true,
  "data": {
    "instance_id": "echo"
  }
}
```

其他读取接口的 `data` 结构：

- `GET /api/runtime`：runtime 摘要，目前包含 `plugin_count`。
- `GET /api/plugins`：插件列表数组，每项是插件 snapshot。
- `GET /api/plugins/:id`：单个插件 snapshot，包含 `instance_id`、`kind`、`manifest`、`lifecycle`、`health`、`reloadable`。

错误响应：

```json
{
  "ok": false,
  "error": {
    "code": "not_reloadable",
    "message": "plugin `echo` is not reloadable"
  }
}
```

## 用户操作与高级运行时语义

普通用户在 Console 中使用的是启用、停用和 reload；下面的 `start` / `stop` 说明仅用于理解高级 runtime-control API。

- `disable`：设置 `enabled = false`，如果插件正在运行则调用 `stop()`，之后不参与调度。
- `enable`：设置 `enabled = true`，必要时调用 `init()` 和 `start()`。
- `stop`：调用 `stop()`，但不改变 `enabled`。
- `start`：插件 enabled 时调用 `start()`。
- `reload`：当前 Rust 插件返回 `not_reloadable`；未来 WASM 插件会在同一入口做原子替换。

## 安全默认

- 默认只监听 loopback。
- token 必需。
- 状态修改只走 POST。
- runtime control API 只通过 runtime control handle 操作插件，不直接暴露插件实例或 service registry。
