[package]
name = "ayiou-protocol"
version = "0.1.0"
edition = "2021"
description = "JSON-RPC protocol definitions for Ayiou external plugins"
license = "MIT"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
